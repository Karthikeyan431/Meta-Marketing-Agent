# tests/ai-evals

Empty in Phase 1. No AI integration exists yet (explicitly forbidden this phase — see
Hard Restrictions). The golden/adversarial/prompt-injection evaluation suite required by
`AI_EVALUATION.md` starts in Phase 8 (AI Read-Only), once there is an `AIProvider`
implementation and typed read-only tools to evaluate.
