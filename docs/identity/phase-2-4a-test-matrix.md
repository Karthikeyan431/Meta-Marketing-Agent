# Phase 2.4A Authorization Test Matrix

**Document ID:** IDENT-020 | Version 1.0 | Status: Approved (Owner Decision — see `phase-2-4a-decisions.md`) | Phase: 2.4A (Architecture Finalization)

Implementation-ready test matrix per the governing task's explicit structure. Each row is
either **EXISTING** (already implemented and passing as of Phase 2.3, commit `fab40e6` —
cites the exact test file, no new work) or **REQUIRED (Phase 2.4)** (specified here in full
so Phase 2.4 implementation can write the test without further design discussion). No test
is implemented by this document — Phase 2.4A is architecture only.

## Authentication

| #   | Case                                                         | Status   | Reference                                                               |
| --- | ------------------------------------------------------------ | -------- | ----------------------------------------------------------------------- |
| A1  | Unauthenticated request → `401 AUTHENTICATION_ERROR`         | EXISTING | `apps/api/src/plugins/auth.test.ts`, `tests/integration/api-me.test.ts` |
| A2  | Authenticated request with valid session → identity resolved | EXISTING | `tests/integration/api-me.test.ts`                                      |

## Workspace

| #   | Case                                                                                                                                                                         | Status               | Reference                                                                                                                                                                                                                                                                                                             |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| W1  | Valid, active workspace → resolves                                                                                                                                           | EXISTING             | `tests/integration/api-workspaces.test.ts`                                                                                                                                                                                                                                                                            |
| W2  | Invalid/nonexistent workspace ID → `403`, no enumeration signal                                                                                                              | EXISTING             | `tests/integration/api-workspaces.test.ts` ("IDOR/BOLA... fabricated/nonexistent workspace ID")                                                                                                                                                                                                                       |
| W3  | Workspace belonging to another user (caller has no membership) → `403`, identical response shape to W2                                                                       | EXISTING             | `tests/integration/api-workspaces.test.ts` ("forged workspace ID")                                                                                                                                                                                                                                                    |
| W4  | Deleted/suspended workspace → `403`, identical to "not a member" (per `authorization.md` §3's disclosure policy — existence of a suspended workspace is not itself revealed) | REQUIRED (Phase 2.4) | Not yet tested — no workspace-deletion route exists yet (Phase 2.3 scope excluded workspace lifecycle mutation endpoints). Write against `requireWorkspaceMembership()` directly: seed a workspace with `status: "SUSPENDED"` or `"DELETED"`, assert membership lookup still returns `403` even for an actual member. |

## Membership

| #   | Case                                                                                                                                   | Status   | Reference                                                                                          |
| --- | -------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------- |
| M1  | Valid, active member → resolves with correct role                                                                                      | EXISTING | `tests/integration/identity-workspace-membership.test.ts`, `api-me.test.ts`                        |
| M2  | Removed member (status `REMOVED`) → `403`, identical to never having joined                                                            | EXISTING | `tests/integration/api-workspaces.test.ts` ("workspace switching attack")                          |
| M3  | Membership that exists but in a _different_ workspace than the one being accessed → `403`/`404` per resource type, never cross-applied | EXISTING | `tests/integration/identity-workspace-membership.test.ts` ("cross-workspace membership isolation") |

## Roles

| #   | Case                                                                                   | Status                  | Reference                                                          |
| --- | -------------------------------------------------------------------------------------- | ----------------------- | ------------------------------------------------------------------ |
| R1  | OWNER — full permission set per `rbac.md` §3                                           | EXISTING (matrix-level) | `packages/domain/src/rbac-catalog.test.ts`                         |
| R2  | ADMIN — full operational set, excluding `workspace.delete`                             | EXISTING                | `rbac-catalog.test.ts`                                             |
| R3  | MANAGER — campaign management, excludes `ai.execute`/`budget.approve`/`budget.execute` | EXISTING                | `rbac-catalog.test.ts` ("financial separation rule")               |
| R4  | ANALYST — read + reporting, no mutation                                                | EXISTING                | `rbac-catalog.test.ts`                                             |
| R5  | VIEWER — read-only, denied every mutation permission                                   | EXISTING                | `rbac-catalog.test.ts` ("denies VIEWER every mutation permission") |

## Permissions

| #   | Case                                                                                         | Status   | Reference                                                                                   |
| --- | -------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------- |
| P1  | Allowed permission for the role → passes                                                     | EXISTING | `apps/api/src/plugins/authorization.test.ts`                                                |
| P2  | Denied permission for the role → `403 AUTHORIZATION_ERROR`                                   | EXISTING | `authorization.test.ts`                                                                     |
| P3  | Unknown/malformed permission key → `false`/deny, never a thrown type error or a silent allow | EXISTING | `rbac-catalog.test.ts` ("returns false for an unknown permission key rather than throwing") |

## Resource

| #   | Case                                                               | Status   | Reference                                                                     |
| --- | ------------------------------------------------------------------ | -------- | ----------------------------------------------------------------------------- |
| X1  | Resource belonging to the current, authorized workspace → resolves | EXISTING | `authorization.test.ts` ("returns the resource when its workspaceId matches") |
| X2  | Resource belonging to a different workspace → `404`, never `403`   | EXISTING | `authorization.test.ts` ("IDOR/BOLA... throws 404")                           |
| X3  | Nonexistent resource → `404`, response indistinguishable from X2   | EXISTING | `authorization.test.ts` ("throws the same 404 for a null resource")           |

## Escalation

| #   | Case                                                                                                                      | Status               | Reference                                                                                                                                                                                                                      |
| --- | ------------------------------------------------------------------------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| E1  | Self role elevation (a member attempts to change their own role, to any value including one they'd otherwise qualify for) | REQUIRED (Phase 2.4) | New: assert `changeMembershipRole()`/its future route reject any call where `actorUserId === target membership.userId`, regardless of the requested role. See `rbac.md` §8.2 rule 3.                                           |
| E2  | ADMIN → OWNER attempt via `changeMembershipRole()` (not `transferOwnership()`)                                            | REQUIRED (Phase 2.4) | New: assert `changeMembershipRole({ newRole: "OWNER", ... })` is rejected unconditionally, for any caller. See `rbac.md` §8.2 rule 1 — this is the single most important new test Phase 2.4A identified.                       |
| E3  | MANAGER → ADMIN attempt (member-management action attempted by a role with no `members.update`)                           | REQUIRED (Phase 2.4) | New: a MANAGER-role membership calling any future member-management route → `403` before any role-comparison logic runs (permission check alone suffices, per `rbac.md` §8.1).                                                 |
| E4  | Client role spoof (role/permission claimed in request body/header/query, ignored)                                         | REQUIRED (Phase 2.4) | New, but same pattern as the already-existing `x-user-id` spoof test (`tests/integration/api-me.test.ts`) — extend to a `role`/`permission`-shaped field once any route accepts a request body that could plausibly carry one. |
| E5  | Client workspace spoof                                                                                                    | EXISTING             | `tests/integration/api-workspaces.test.ts` (forged workspace ID tests, W2/W3 above)                                                                                                                                            |

## Concurrency

| #   | Case                                                                                    | Status               | Reference                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | --------------------------------------------------------------------------------------- | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Simultaneous ownership changes (two owners, both removed concurrently)                  | EXISTING             | `tests/integration/identity-owner-invariant.test.ts` ("never reaches zero owners under concurrent owner-removal attempts")                                                                                                                                                                                                                                                                                     |
| C2  | Simultaneous role changes to the same non-owner-affecting membership                    | REQUIRED (Phase 2.4) | New — see `identity-threat-model.md` threat #19; fire two concurrent `changeMembershipRole()` calls at one membership, assert the final state is exactly one of the two attempted values, never corrupted.                                                                                                                                                                                                     |
| C3  | Membership deletion during an in-flight authorization check (the classic TOCTOU window) | REQUIRED (Phase 2.4) | New — structurally mitigated already (every `requireWorkspaceMembership()` call re-queries fresh within its own request), but write an explicit test: begin a slow request, remove the membership mid-flight, assert the in-flight request's _authorization-dependent_ outcome reflects the removal if the DB query happens after the removal commits (no stale in-memory role is ever held across the check). |
| C4  | Concurrent transfer-vs-removal targeting the same owner                                 | EXISTING             | `identity-owner-invariant.test.ts`                                                                                                                                                                                                                                                                                                                                                                             |

## AI (architecture-only in Phase 2.4A — tests below are specified for whichever phase implements AI tools)

| #   | Case                                                                                                                                                                   | Status                     | Reference                                                                                                                                                                       |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AI1 | AI attempts an unauthorized tool (the initiating user lacks the mapped permission) → the tool call fails exactly as the equivalent direct API call would for that user | REQUIRED (future AI phase) | Spec: `ai-authorization-contract.md` §3/§4. No AI code exists yet to test.                                                                                                      |
| AI2 | AI attempts a cross-workspace resource (references a resource ID from a different workspace than the active one)                                                       | REQUIRED (future AI phase) | Spec: `ai-authorization-contract.md` §4 step 6 — must resolve `404`, identical to X2/X3 above.                                                                                  |
| AI3 | AI attempts a financial operation without the initiating user holding the permission                                                                                   | REQUIRED (future AI phase) | Spec: `ai-authorization-contract.md` §5 — `budget.propose` is the AI's ceiling; `budget.approve`/`budget.execute` are marked **Never** AI-invocable in `permission-catalog.md`. |
| AI4 | AI attempts to bypass approval (proposes and tries to self-execute a High Risk action)                                                                                 | REQUIRED (future AI phase) | Spec: `ai-authorization-contract.md` §3's mandatory sequence — Execution can only follow Approval; no tool exists that skips from Propose directly to Execute.                  |

## Workers (architecture-only in Phase 2.4A for the five not-yet-implemented workers)

| #   | Case                                                                                                                | Status                                                            | Reference                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| WK1 | Valid worker context (workspace + actor both currently valid) → job executes                                        | EXISTING (for the one implemented worker)                         | `workers/webhook/src/processor.test.ts` (job-name dispatch), `tests/integration/identity-sync.test.ts` (the actual sync logic a job invokes)           |
| WK2 | Invalid workspace context (job payload references a deleted/nonexistent workspace)                                  | REQUIRED (Phase 2.4, whichever future worker mutates tenant data) | Spec: `worker-authorization-contract.md` §3/§4.                                                                                                        |
| WK3 | Revoked actor (the `initiatingActor` was removed from the workspace since the job was enqueued)                     | REQUIRED (Phase 2.4+)                                             | Spec: `worker-authorization-contract.md` §4's table, row 1.                                                                                            |
| WK4 | Changed role (actor's role was downgraded since enqueue; job's `actionScope` no longer matches current permissions) | REQUIRED (Phase 2.4+)                                             | Spec: `worker-authorization-contract.md` §4's table, row 3.                                                                                            |
| WK5 | Retry after revocation (job fails auth on attempt 1's retry, must not eventually succeed via blind retry)           | REQUIRED (Phase 2.4+)                                             | Spec: `worker-authorization-contract.md` §4's table, row 5 — distinguish transient-retry from authorization-denial-stop, per `WORKER_ARCHITECTURE.md`. |

## Failure Behavior (cross-cutting — verified per endpoint, not a separate suite)

| #   | Case                                                                                                                                                         | Status   | Reference                                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | `401` used exclusively for "no valid session," never for any authorization-layer failure                                                                     | EXISTING | Every integration test above implicitly verifies this by asserting `401` only on the no-session case.                                                                                                                                                                                           |
| F2  | `403` used for "authenticated but not authorized" (no membership, wrong role, wrong permission)                                                              | EXISTING | `authorization.test.ts`, `api-workspaces.test.ts`.                                                                                                                                                                                                                                              |
| F3  | `404` used only where disclosure would enable cross-workspace enumeration (resource-scoped lookups), never as a generic "not found" for auth failures        | EXISTING | `authorization.test.ts` (X2/X3 above).                                                                                                                                                                                                                                                          |
| F4  | No response body (success or error) ever contains a secret, token, or internal implementation detail                                                         | EXISTING | `tests/integration/api-me.test.ts` / `api-webhooks-clerk.test.ts` ("never leaks the configured secret key value").                                                                                                                                                                              |
| F5  | `409 CONFLICT` used for the owner invariant specifically (a request that is well-formed and authorized in principle, but would violate a business invariant) | EXISTING | `OwnerInvariantError`'s `statusCode = 409` (`apps/api/src/plugins/authorization.ts`); exercised at the domain layer in `identity-owner-invariant.test.ts` — not yet exercised through an HTTP route (no member-management route exists yet), flagged as **REQUIRED (Phase 2.4)** once one does. |

## Summary

- **EXISTING, already passing as of commit `fab40e6`:** 22 of 33 matrix rows.
- **REQUIRED (Phase 2.4 implementation):** 9 rows — all fully specified above, no further
  design decisions needed to write them.
- **REQUIRED (future AI/worker phases):** 8 rows (AI1–AI4, WK2–WK5) — specified in
  `ai-authorization-contract.md`/`worker-authorization-contract.md`, blocked only on those
  phases' own feature implementation, not on any unresolved architecture question here.
