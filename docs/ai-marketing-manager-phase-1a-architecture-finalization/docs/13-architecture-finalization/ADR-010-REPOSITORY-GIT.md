# ADR-010 — Repository Initialization
**Status:** Approved

Initialize Git before Foundation implementation. Add safe ignore rules, editor configuration, repository documentation, protected main branch after remote creation, secret scanning and dependency scanning.

Never commit provider credentials or environment secrets.
